export const TRANSCRIBE_SERVICE = 'TRANSCRIBE SERVICE';
export const TRANSCRIBE_MODULE = 'TRANSCRIBE MODULE';

export const TRANSCRIBE_MESSAGE_PATTERNS = {
  CREATE: 'transcribe.create',
  UPDATE: 'transcribe.update',
  DELETE: 'transcribe.delete'
}

export const TRANSCRIBE_MESSAGE_DB_RESPONSE = {
  ID_EXISTING: 'transcribe.resource_id_existing',
  NOT_FOUND: 'transcribe.resource_id_not_found',
  UPDATED: 'transcribe.resource_has_updated'
}
